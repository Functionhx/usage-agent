{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://functionhx.github.io/usage-agent/data/usage.schema.json",
  "title": "AI coding tool daily usage aggregate",
  "description": "每天由 vibe-local 产出。只含聚合数字：每日总额与按模型拆分。刻意不包含项目名、文件路径或任何 prompt 内容——不是过滤掉的，而是数据结构里根本没有这些字段。",
  "type": "object",
  "additionalProperties": false,
  "required": ["host", "schemaVersion", "updatedAt", "days"],
  "properties": {
    "host": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9._-]+$",
      "description": "产出该文件的机器名。必须与文件名 <host>.json 一致。"
    },
    "schemaVersion": {
      "const": 1
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "UTC 时刻（带 Z），不是日历日。日界见 days[].date。"
    },
    "days": {
      "type": "array",
      "maxItems": 400,
      "description": "按日期升序。日期是 UTC+8 日历日，日界落在 UTC 16:00。",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["date", "cost", "tokens", "cacheRead", "byModel"],
        "properties": {
          "date": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "UTC+8 日历日"
          },
          "cost": {
            "type": "number",
            "minimum": 0,
            "description": "当日估算花费（美元）。无价格的模型按 $0 计，所以这是下界。"
          },
          "tokens": {
            "type": "integer",
            "minimum": 0,
            "description": "不含 cache read —— 与 bucket 的 totalTokens 口径一致。"
          },
          "cacheRead": {
            "type": "integer",
            "minimum": 0,
            "description": "单列，因为它常占大头，混进 tokens 会让数字对不上成本。"
          },
          "byModel": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["model", "cost", "tokens"],
              "properties": {
                "model": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 100
                },
                "cost": { "type": "number", "minimum": 0 },
                "tokens": { "type": "integer", "minimum": 0 }
              }
            }
          }
        }
      }
    }
  }
}
